namespace PathOfExile.GameClient.Monitor.Notifications
{
    public enum ChatChannel
    {
        Local,
        Global,
        Trade,
        Guild,
        WhisperTo,
        WhisperFrom,
        Party
    }
}